export interface EffectConfig {
    id: string;
    name: string;
    hasEffect: boolean ;
    duration: number;
    inRowId: string;
    startTime: number;
    endTime: number;
    effectType: string;
}